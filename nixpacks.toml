# Developer: Smith - Mustafa Hussein

[providers]
python = "3.10"

[phases.setup]
nixPkgs = ["python310"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python app.py"
